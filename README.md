# HDDchangepoint

You can simply install this R package by copying the following two commands in your Rstudio:

library(devtools)

install_github("rezadrikvandi/HDDchangepoint")

More information about this package can be found in the DESCRIPTION file etc.
